<!DOCTYPE html>
<html lang="en">

<head>
	<title>CMS Task Manager</title>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body style="background: lightyellow">
	<center>
		<h1><em><%= @project.name %></em></h1>
		<table>
			<tr>
				<th>Task Name</th>
				<th>Complete</th>
				<th>Due Date</th>
			</tr>
			<% @project.project_tasks.each do |t| %>
			<tr>
				<td><a href="/tasks/<%= t.id %>"><%= t.name %></a></td>
				<td>
					<input type="checkbox" value="false" disabled="disabled" />
				</td>
				<td>
					<input type="datetime" disabled="disabled" value="<%=Time.now%>" />
				</td>
			</tr>
			<% end %>
		</table>
	</center>
</body>

</html>