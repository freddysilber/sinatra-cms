<!DOCTYPE html>
<html lang="en">

<head>
	<title>CMS Task Manager</title>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body style="background: lightyellow">
	<center>
		<h1 style="color: red">Are you sure you want to delete this project? - <em><%= @project.name %></em></h1>
		<table>
			<tr>
				<th>Task Name</th>
				<th>Complete</th>
				<th>Due Date</th>
			</tr>
			<% @project.project_tasks.each do |t| %>
			<tr>
				<td><em><a href="/tasks/<%= t.id %>"><%= t.name %></a></em></td>
				<td>
					<%if t.complete == true %>
					<input type="checkbox" disabled="disabled" checked %>
					<%else%>
					<input type="checkbox" disabled="disabled" />
					<%end%>
				</td>
				<td>
					<p><%=t.due_date%></p>
				</td>
			</tr>
			<% end %>
		</table>
		<br/>
		<br/>
		<br/>
		<form action="/projects/<%= @project.id %>/delete" method="post">
			<input id="hidden" type="hidden" name="_method" value="delete">
			<input type="submit" value="Delete">
		</form>
	</center>
</body>

</html>